[project]
name = "tandem-race-software"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "bottle>=0.13.4",
    "pyautogui>=0.9.54",
    "waitress>=3.0.2",
]

[dependency-groups]
dev = [
    "hupper>=1.12.1",
]
